# STDD Copilot - AI Agent Instructions

> Version: 1.0 | Last Updated: 2026-05-13

## Overview

STDD Copilot (Spec + Test Driven Development) is a unified framework.

## 全部能力入口 (45 个 = 20 Command 模板 + 38 Skill 模板，去重后)

### Command 模板入口 (20)
- `/stdd:init`, `/stdd:new`, `/stdd:propose`, `/stdd:clarify`, `/stdd:confirm`
- `/stdd:spec`, `/stdd:plan`, `/stdd:apply`, `/stdd:execute`, `/stdd:verify`, `/stdd:archive`
- `/stdd:final-doc`, `/stdd:brainstorm`, `/stdd:issue`, `/stdd:constitution`
- `/stdd:ff`
- `/stdd:continue`
- `/stdd:explore`
- `/stdd:graph`
- `/stdd:turbo`

### SDD & TDD 核心流程
- `/stdd:new`, `/stdd:apply`, `/stdd:verify`, `/stdd:archive`

### 辅助功能
- `/stdd:constitution`, `/stdd:guard`, `/stdd:metrics`, `/stdd:learn`, `/stdd:roles` ...
